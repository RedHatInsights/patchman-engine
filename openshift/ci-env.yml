# this will be contained in the buildfactory part of e2e-deploy
build:
  parameters:
    SOURCE_REPOSITORY_URL: https://github.com/RedHatInsights/patchman-engine.git
    SOURCE_REPOSITORY_REF: master

# When migrating to e2e-deploy this will be renamed to patchman or smth similar
deploy:
  parameters:
    IMAGE_NAMESPACE: patchman-engine-ci
    KAFKA_ADDRESS: "FILL ME"
    INVENTORY_ADDRESS: "FILL ME"
    VMAAS_ADDRESS: "FILL ME"

